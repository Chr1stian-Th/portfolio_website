/**
 * Lucide icon registry.
 *
 * Each entry's content file may set `icon: 'graduation'` (or any other key
 * here) to override the default file/folder icon. Add more icons to this
 * map and they become available to the schema.
 */
import {
  FileText,
  Folder,
  FolderOpen,
  GraduationCap,
  Beer,
  Activity,
  User,
  Briefcase,
  BookOpen,
  Search,
  Wrench,
} from 'lucide-react';

export const iconMap = {
  file:       FileText,
  folder:     Folder,
  folderOpen: FolderOpen,
  graduation: GraduationCap,
  beer:       Beer,
  activity:   Activity,
  user:       User,
  briefcase:  Briefcase,
  book:       BookOpen,
  search:     Search,
  wrench:     Wrench,
};
